base:
  language: c++
  steps:
    - run: $CXX $CXXFLAGS -c ./api.h ./api.cpp
    - run: $CXX $CXXFLAGS ./api.o ./harness.cpp $LIB_FUZZING_ENGINE -o $OUT/broken_method_fuzzer
  sanitizers:
    - address:
    - undefined:
    - thread: